# Open Contexts 术语表

- **应用**：普通运行中的应用，包括没有窗口的应用；不包括纯菜单栏或后台应用。
- **窗口**：属于应用的单个可切换窗口，也是窗口切换器的切换粒度。
- **窗口侧栏（用户称应用栏/Dock）**：在每个显示器上展示所有窗口的侧栏；点击窗口行会直接激活该窗口。同一应用的窗口不要求相邻。
- **窗口切换器**：用于选择和切换单个窗口的界面，替代系统窗口切换体验。
- **分组**：可由用户命名、拖拽排序的持久窗口集合；即使为空也始终显示。同一应用的窗口可属于不同分组。
