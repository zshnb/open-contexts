# ADR 0002：窗口为分组与排序粒度

## 决定

分组和拖拽排序以窗口为单位。同一应用的窗口可以分属不同分组，并分别排序；无窗口应用不显示侧栏条目。

## 原因

这是用户明确要求，应用级成员关系无法表达该行为。

## 结果

跨重启采用尽力恢复：应用、文档 URL 或标题可唯一匹配时恢复分组和顺序；歧义或无法匹配时进入“未分组”。这保留窗口级体验，但无法保证重新打开窗口的身份完全一致。
